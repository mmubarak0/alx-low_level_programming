#include "main.h"

/**
  * _strpbrk - something
  * @s: string or what?
  * @accept: string or what?
  * Return: string also hhh
  */
char *_strpbrk(char *s, char *accept)
{
	_putchar(*accept);
	return (s);
}
